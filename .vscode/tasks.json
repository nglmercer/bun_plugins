{
	"version": "2.0.0",
	"tasks": [
		{
			"type": "shell",
			"label": "compile-extension",
			"command": "bun run compile",
			"options": {
				"cwd": "${workspaceFolder}/vscode-extension"
			},
			"group": {
				"kind": "build",
				"isDefault": true
			},
			"problemMatcher": "$tsc"
		}
	]
}
